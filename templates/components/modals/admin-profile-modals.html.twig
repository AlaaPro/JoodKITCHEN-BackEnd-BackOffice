<!-- Create Admin Profile Modal -->
<div class="modal fade" id="createAdminModal" tabindex="-1" aria-labelledby="createAdminModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header bg-primary text-white">
                <h5 class="modal-title" id="createAdminModalLabel">
                    <i class="fas fa-user-shield"></i> Nouvel Administrateur
                </h5>
                <button type="button" class="btn-close btn-close-white" data-coreui-dismiss="modal" aria-label="Close"></button>
            </div>
            <form id="createAdminForm" novalidate>
                <div class="modal-body">
                    <!-- Security Warning -->
                    <div class="alert alert-warning mb-4">
                        <i class="fas fa-exclamation-triangle"></i>
                        <strong>Zone sécurisée :</strong> Vous créez un compte administrateur avec des privilèges élevés.
                    </div>

                    <div class="row">
                        <!-- Basic Information -->
                        <div class="col-md-6">
                            <h6 class="fw-bold mb-3"><i class="fas fa-user"></i> Informations Personnelles</h6>
                            
                            <div class="mb-3">
                                <label for="createAdminNom" class="form-label">Nom <span class="text-danger">*</span></label>
                                <input type="text" class="form-control" id="createAdminNom" name="nom" required>
                                <div class="invalid-feedback">Veuillez saisir le nom.</div>
                            </div>

                            <div class="mb-3">
                                <label for="createAdminPrenom" class="form-label">Prénom <span class="text-danger">*</span></label>
                                <input type="text" class="form-control" id="createAdminPrenom" name="prenom" required>
                                <div class="invalid-feedback">Veuillez saisir le prénom.</div>
                            </div>

                            <div class="mb-3">
                                <label for="createAdminEmail" class="form-label">Email <span class="text-danger">*</span></label>
                                <input type="email" class="form-control" id="createAdminEmail" name="email" required>
                                <div class="invalid-feedback">Veuillez saisir un email valide.</div>
                            </div>

                            <div class="mb-3">
                                <label for="createAdminTelephone" class="form-label">Téléphone</label>
                                <input type="tel" class="form-control" id="createAdminTelephone" name="telephone">
                            </div>
                        </div>

                        <!-- Security & Roles -->
                        <div class="col-md-6">
                            <h6 class="fw-bold mb-3"><i class="fas fa-shield-alt"></i> Sécurité & Rôles</h6>
                            
                            <div class="mb-3">
                                <label for="createAdminPassword" class="form-label">Mot de passe <span class="text-danger">*</span></label>
                                <div class="input-group">
                                    <input type="password" class="form-control" id="createAdminPassword" name="password" required>
                                    <button class="btn btn-outline-secondary" type="button" onclick="togglePassword('createAdminPassword')">
                                        <i class="fas fa-eye"></i>
                                    </button>
                                </div>
                                <div class="form-text">Minimum 8 caractères avec lettres et chiffres</div>
                                <div class="invalid-feedback">Mot de passe requis (min. 8 caractères).</div>
                            </div>

                            <div class="mb-3">
                                <label for="createAdminRole" class="form-label">Niveau d'accès <span class="text-danger">*</span></label>
                                <select class="form-select" id="createAdminRole" name="role" required>
                                    <option value="">Sélectionner le niveau</option>
                                    <option value="ROLE_ADMIN">Administrateur</option>
                                    <option value="ROLE_SUPER_ADMIN" disabled>Super Administrateur (Réservé)</option>
                                </select>
                                <div class="invalid-feedback">Veuillez sélectionner un niveau d'accès.</div>
                            </div>

                            <div class="mb-3">
                                <label class="form-label">Rôles Internes</label>
                                <div id="createAdminRolesInternes" class="form-check-container">
                                    <!-- Dynamically populated -->
                                </div>
                                <div class="form-text">Rôles spécifiques dans l'organisation</div>
                            </div>

                            <div class="mb-3">
                                <div class="form-check form-switch">
                                    <input class="form-check-input" type="checkbox" id="createAdminIsActive" name="is_active" checked>
                                    <label class="form-check-label" for="createAdminIsActive">
                                        Compte actif
                                    </label>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Advanced Permissions -->
                    <div class="row mt-4">
                        <div class="col-12">
                            <h6 class="fw-bold mb-3"><i class="fas fa-key"></i> Permissions Avancées</h6>
                            <div id="createAdminPermissions" class="permissions-grid">
                                <!-- Dynamically populated -->
                            </div>
                        </div>
                    </div>

                    <!-- Internal Notes -->
                    <div class="row mt-4">
                        <div class="col-12">
                            <h6 class="fw-bold mb-3"><i class="fas fa-sticky-note"></i> Notes Internes</h6>
                            <div class="mb-3">
                                <textarea class="form-control" id="createAdminNotes" name="notes_interne" rows="3" 
                                          placeholder="Notes internes (visibles uniquement par les administrateurs)"></textarea>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-coreui-dismiss="modal">
                        <i class="fas fa-times"></i> Annuler
                    </button>
                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-save"></i> Créer l'Administrateur
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Edit Admin Profile Modal -->
<div class="modal fade" id="editAdminModal" tabindex="-1" aria-labelledby="editAdminModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header bg-success text-white">
                <h5 class="modal-title" id="editAdminModalLabel">
                    <i class="fas fa-user-edit"></i> Modifier l'Administrateur
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <form id="editAdminForm" novalidate>
                <input type="hidden" id="editAdminId" name="admin_id">
                
                <div class="modal-body">
                    <!-- Admin Info Header -->
                    <div class="alert alert-info mb-4">
                        <div class="d-flex align-items-center">
                            <div class="avatar-circle me-3">
                                <i class="fas fa-user-shield"></i>
                            </div>
                            <div>
                                <strong id="editAdminFullName">Nom Complet</strong><br>
                                <small id="editAdminCurrentEmail">email@example.com</small>
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <!-- Basic Information -->
                        <div class="col-md-6">
                            <h6 class="fw-bold mb-3"><i class="fas fa-user"></i> Informations Personnelles</h6>
                            
                            <div class="mb-3">
                                <label for="editAdminNom" class="form-label">Nom <span class="text-danger">*</span></label>
                                <input type="text" class="form-control" id="editAdminNom" name="nom" required>
                                <div class="invalid-feedback">Veuillez saisir le nom.</div>
                            </div>

                            <div class="mb-3">
                                <label for="editAdminPrenom" class="form-label">Prénom <span class="text-danger">*</span></label>
                                <input type="text" class="form-control" id="editAdminPrenom" name="prenom" required>
                                <div class="invalid-feedback">Veuillez saisir le prénom.</div>
                            </div>

                            <div class="mb-3">
                                <label for="editAdminEmail" class="form-label">Email <span class="text-danger">*</span></label>
                                <input type="email" class="form-control" id="editAdminEmail" name="email" required>
                                <div class="invalid-feedback">Veuillez saisir un email valide.</div>
                            </div>

                            <div class="mb-3">
                                <label for="editAdminTelephone" class="form-label">Téléphone</label>
                                <input type="tel" class="form-control" id="editAdminTelephone" name="telephone">
                            </div>
                        </div>

                        <!-- Security & Roles -->
                        <div class="col-md-6">
                            <h6 class="fw-bold mb-3"><i class="fas fa-shield-alt"></i> Sécurité & Rôles</h6>
                            
                            <div class="mb-3">
                                <label for="editAdminPassword" class="form-label">Nouveau mot de passe</label>
                                <div class="input-group">
                                    <input type="password" class="form-control" id="editAdminPassword" name="password" 
                                           placeholder="Laisser vide pour conserver">
                                    <button class="btn btn-outline-secondary" type="button" onclick="togglePassword('editAdminPassword')">
                                        <i class="fas fa-eye"></i>
                                    </button>
                                </div>
                                <div class="form-text">Laisser vide pour conserver le mot de passe actuel</div>
                            </div>

                            <div class="mb-3">
                                <label for="editAdminRole" class="form-label">Niveau d'accès <span class="text-danger">*</span></label>
                                <select class="form-select" id="editAdminRole" name="role" required>
                                    <option value="">Sélectionner le niveau</option>
                                    <option value="ROLE_ADMIN">Administrateur</option>
                                    <option value="ROLE_SUPER_ADMIN">Super Administrateur</option>
                                </select>
                                <div class="invalid-feedback">Veuillez sélectionner un niveau d'accès.</div>
                            </div>

                            <div class="mb-3">
                                <label class="form-label">Rôles Internes</label>
                                <div id="editAdminRolesInternes" class="form-check-container">
                                    <!-- Dynamically populated -->
                                </div>
                            </div>

                            <div class="mb-3">
                                <div class="form-check form-switch">
                                    <input class="form-check-input" type="checkbox" id="editAdminIsActive" name="is_active">
                                    <label class="form-check-label" for="editAdminIsActive">
                                        Compte actif
                                    </label>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Advanced Permissions -->
                    <div class="row mt-4">
                        <div class="col-12">
                            <h6 class="fw-bold mb-3"><i class="fas fa-key"></i> Permissions Avancées</h6>
                            <div id="editAdminPermissions" class="permissions-grid">
                                <!-- Dynamically populated -->
                            </div>
                        </div>
                    </div>

                    <!-- Internal Notes -->
                    <div class="row mt-4">
                        <div class="col-12">
                            <h6 class="fw-bold mb-3"><i class="fas fa-sticky-note"></i> Notes Internes</h6>
                            <div class="mb-3">
                                <textarea class="form-control" id="editAdminNotes" name="notes_interne" rows="3" 
                                          placeholder="Notes internes (visibles uniquement par les administrateurs)"></textarea>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                        <i class="fas fa-times"></i> Annuler
                    </button>
                    <button type="submit" class="btn btn-success">
                        <i class="fas fa-save"></i> Enregistrer les Modifications
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- View Admin Profile Modal -->
<div class="modal fade" id="viewAdminModal" tabindex="-1" aria-labelledby="viewAdminModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header bg-info text-white">
                <h5 class="modal-title" id="viewAdminModalLabel">
                    <i class="fas fa-user-circle"></i> Profil Administrateur
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <!-- Admin Profile Details -->
                <div id="adminProfileDetails">
                    <!-- Dynamically populated -->
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                    <i class="fas fa-times"></i> Fermer
                </button>
                <button type="button" class="btn btn-primary" id="editFromViewBtn">
                    <i class="fas fa-edit"></i> Modifier
                </button>
            </div>
        </div>
    </div>
</div>

<!-- Delete Confirmation Modal -->
<div class="modal fade" id="deleteAdminModal" tabindex="-1" aria-labelledby="deleteAdminModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-danger text-white">
                <h5 class="modal-title" id="deleteAdminModalLabel">
                    <i class="fas fa-trash"></i> Confirmer la Suppression
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div class="alert alert-danger">
                    <i class="fas fa-exclamation-triangle"></i>
                    <strong>Attention !</strong> Cette action est irréversible.
                </div>
                <p>Êtes-vous sûr de vouloir supprimer l'administrateur <strong id="deleteAdminName">Nom</strong> ?</p>
                <p class="text-muted">Toutes les données associées seront définitivement supprimées.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                    <i class="fas fa-times"></i> Annuler
                </button>
                <button type="button" class="btn btn-danger" id="confirmDeleteAdminBtn">
                    <i class="fas fa-trash"></i> Supprimer Définitivement
                </button>
            </div>
        </div>
    </div>
</div>

<!-- Bulk Actions Modal -->
<div class="modal fade" id="bulkActionsModal" tabindex="-1" aria-labelledby="bulkActionsModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-warning text-dark">
                <h5 class="modal-title" id="bulkActionsModalLabel">
                    <i class="fas fa-tasks"></i> Actions en Lot
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p>Sélectionnez une action à appliquer à <strong id="selectedAdminsCount">0</strong> administrateur(s) :</p>
                
                <div class="d-grid gap-2">
                    <button class="btn btn-outline-success" id="bulkActivateBtn">
                        <i class="fas fa-user-check"></i> Activer les Comptes
                    </button>
                    <button class="btn btn-outline-warning" id="bulkSuspendBtn">
                        <i class="fas fa-user-times"></i> Suspendre les Comptes
                    </button>
                    <button class="btn btn-outline-info" id="bulkExportBtn">
                        <i class="fas fa-download"></i> Exporter la Sélection
                    </button>
                    <hr>
                    <button class="btn btn-outline-danger" id="bulkDeleteBtn">
                        <i class="fas fa-trash"></i> Supprimer la Sélection
                    </button>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                    <i class="fas fa-times"></i> Annuler
                </button>
            </div>
        </div>
    </div>
</div>

<style>
.permissions-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 10px;
}

.form-check-container {
    max-height: 200px;
    overflow-y: auto;
    border: 1px solid var(--bs-border-color);
    border-radius: 0.375rem;
    padding: 0.75rem;
}

.avatar-circle {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: var(--bs-primary);
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
}

.avatar-circle img {
    width: 100%;
    height: 100%;
    border-radius: 50%;
    object-fit: cover;
}

.activity-icon {
    width: 32px;
    height: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.75rem;
}
</style>

<script>
function togglePassword(inputId) {
    const input = document.getElementById(inputId);
    const icon = input.nextElementSibling.querySelector('i');
    
    if (input.type === 'password') {
        input.type = 'text';
        icon.classList.remove('fa-eye');
        icon.classList.add('fa-eye-slash');
    } else {
        input.type = 'password';
        icon.classList.remove('fa-eye-slash');
        icon.classList.add('fa-eye');
    }
}
</script> 